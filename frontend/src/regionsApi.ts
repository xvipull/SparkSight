import type { AnalyticsRow, GlobalFilters, Overview } from './types'
export interface RegionData { overview: Overview; regions: AnalyticsRow[] }
export async function requestRegions(filters: GlobalFilters): Promise<RegionData> { const params = new URLSearchParams(); Object.entries(filters).forEach(([key,value])=>{if(value)params.set(key,value)}); const suffix=params.toString()?`?${params}`:''; const [overview,regions]=await Promise.all([fetch(`/api/overview${suffix}`),fetch(`/api/regions${suffix}`)]); if(!overview.ok||!regions.ok)throw new Error('Region analytics request failed'); return {overview:await overview.json() as Overview,regions:await regions.json() as AnalyticsRow[]} }
