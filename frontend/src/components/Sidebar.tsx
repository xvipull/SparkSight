import { BarChart3, Boxes, ChevronLeft, Database, GitBranch, Map, Package, Sparkles, TrendingUp, Users } from 'lucide-react'
const links = [[BarChart3, 'Overview'], [TrendingUp, 'Sales Trends'], [Package, 'Products'], [Users, 'Customers'], [Map, 'Regions'], [Database, 'Data Pipeline']]
export function Sidebar({ collapsed, toggle }: { collapsed: boolean; toggle: () => void }) { return <aside className={`sidebar ${collapsed ? 'collapsed' : ''}`}><div className="sidebar-top"><a className="brand" href="#overview"><span className="brand-icon"><Sparkles size={17}/></span><b>SparkSight</b></a><button className="collapse" onClick={toggle} aria-label="Toggle sidebar"><ChevronLeft size={16}/></button></div><nav>{links.map(([Icon, label], index) => { const ItemIcon = Icon as typeof BarChart3; return <a href={`#${String(label).toLowerCase().replace(' ', '-')}`} className={index === 0 ? 'selected' : ''} key={String(label)}><ItemIcon size={18}/><span>{String(label)}</span></a> })}</nav><div className="sidebar-bottom"><div className="system"><span className="status-dot"/><div><strong>System status</strong><small>Pipeline operational</small></div></div><a className="github" href="https://github.com/xvipul/SparkSight" target="_blank" rel="noreferrer"><GitBranch size={17}/><span>GitHub repository</span></a><div className="spark-engine"><Boxes size={16}/><span>Apache Spark Engine</span></div></div></aside> }
