import { CalendarDays, RefreshCw } from 'lucide-react'
export function Header({ range, refreshing, refresh }: { range: string; refreshing: boolean; refresh: () => void }) { return <header className="header"><div><p className="eyebrow">ANALYTICS WORKSPACE</p><h1>Sales Analytics</h1><p>Distributed sales intelligence powered by Apache Spark</p></div><div className="header-actions"><div className="date-range"><CalendarDays size={16}/><span>{range}</span></div><button className="refresh" onClick={refresh} disabled={refreshing}><RefreshCw className={refreshing ? 'spin' : ''} size={16}/>{refreshing ? 'Refreshing' : 'Refresh Data'}</button></div></header> }
